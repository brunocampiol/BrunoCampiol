@page
@model BrunoCampiol.UI.Web.Pages.SettingsModel


<div class="jumbotron text-center">
    <h1 class="align-middle">Bruno Settings <span class="fas fa-cogs" style="color:DodgerBlue;"></span></h1>
</div>

<br />

<div class="container">
    <div class="table-responsive">
        <table class="table global-settings-table">
            <thead class="thead-light">
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Setting</th>
                    <th scope="col">Value</th>
                </tr>
            </thead>
            <tbody>
                @{ int order = 1; }
                @foreach (var item in ((List<(string Key, string Value)>)ViewData["SettingsList"]))
                {
                    <tr>
                        <th scope="row">@order</th>
                        <td>@item.Key</td>
                        <td>@item.Value</td>
                    </tr>

                    order++;
                }
            </tbody>
        </table>
    </div>
</div>